# 小白友好部署说明

## 🎯 部署前必读

这份说明专门为没有技术经验的小白用户编写。请按照步骤一步一步操作，不要跳过任何步骤。

### 📋 你需要准备什么

1. **一台服务器**（可以是云服务器，比如阿里云、腾讯云、华为云等）
2. **服务器系统**：推荐使用 Ubuntu 20.04 或 CentOS 7/8
3. **一个域名**（可选，但如果要公网访问建议有）
4. **1小时左右的时间**（第一次部署可能会慢一些）

---

## 🚀 第一步：连接到你的服务器

### 1.1 下载连接工具

如果你用的是 Windows 系统：
- 下载 [PuTTY](https://www.putty.org/) （免费的SSH连接工具）
- 或者下载 [Xshell](https://www.netsarang.com/zh/xshell/) （有免费版）

如果你用的是 Mac 系统：
- 直接使用系统自带的"终端"（Terminal）

### 1.2 获取服务器信息

登录你的云服务器控制台，找到以下信息：
- **服务器IP地址**（比如：123.456.789.123）
- **用户名**（通常是：root）
- **密码**（你设置的密码）

### 1.3 连接服务器

**Windows + PuTTY 用户：**
1. 打开 PuTTY
2. 在 "Host Name" 中输入你的服务器IP地址
3. 点击 "Open"
4. 会弹出黑色窗口，输入用户名（root）然后回车
5. 输入密码（输入时不会显示，直接输完回车）

**Mac + 终端用户：**
1. 打开"终端"
2. 输入：`ssh root@你的服务器IP`（比如：`ssh root@123.456.789.123`）
3. 回车后输入密码（输入时不会显示）

---

## 📦 第二步：安装必要软件

### 2.1 更新系统

连接成功后，在黑色窗口中输入以下命令（一行一行输入，每输完一行按回车）：

**Ubuntu 系统：**
```bash
apt update
apt upgrade -y
```

**CentOS 系统：**
```bash
yum update -y
```

### 2.2 安装 Node.js

输入以下命令安装 Node.js：

**Ubuntu 系统：**
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs
```

**CentOS 系统：**
```bash
curl -fsSL https://rpm.nodesource.com/setup_18.x | bash -
yum install -y nodejs
```

### 2.3 安装 Git

**Ubuntu 系统：**
```bash
apt install -y git
```

**CentOS 系统：**
```bash
yum install -y git
```

### 2.4 验证安装

输入以下命令，如果都显示版本号说明安装成功：

```bash
node --version
npm --version
git --version
```

---

## 📁 第三步：上传项目文件

### 3.1 下载项目代码

在服务器上输入以下命令：

```bash
cd /root
git clone https://github.com/你的用户名/你的项目名.git
cd 你的项目名
```

**注意**：如果你没有把代码上传到GitHub，可以用以下方法：

**方法1：使用FTP上传**
1. 下载 [FileZilla](https://filezilla-project.org/) （免费的FTP工具）
2. 连接到你的服务器（主机填服务器IP，用户名root，密码你的密码，端口22）
3. 将你的项目文件夹整个拖拽到服务器的 `/root` 目录

**方法2：直接在服务器创建**
如果你有代码文件，可以直接在服务器上创建（不推荐，因为太麻烦）

### 3.2 安装项目依赖

```bash
npm install
```

这个命令会运行几分钟，请耐心等待，不要中断。

---

## 🗄️ 第四步：初始化数据库

### 4.1 创建环境变量文件

先复制一份环境变量模板：

```bash
cp .env.example .env
```

然后编辑 `.env` 文件：

```bash
nano .env
```

把里面的 `JWT_SECRET` 和 `ADMIN_PASSWORD` 改成你自己的内容。例如：

```bash
DATABASE_URL="file:../db/custom.db"
JWT_SECRET="这里换成一串很长的随机字符"
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="这里换成你的强密码"
MAX_UPLOAD_BYTES="104857600"
UPLOAD_DIR="uploads"
```

按 `Ctrl+X`，然后按 `Y`，再按回车保存。

### 4.2 创建数据库

```bash
npm run db:push
```

如果出现错误，可能需要先安装SQLite：

**Ubuntu 系统：**
```bash
apt install -y sqlite3
```

**CentOS 系统：**
```bash
yum install -y sqlite
```

然后再运行：
```bash
npm run db:push
```

### 4.3 创建管理员账号

```bash
npx tsx scripts/create-admin.ts
```

如果成功，你会看到：
```
管理员创建成功:
用户名: admin
```

**请务必记录 `.env` 中设置的管理员用户名和密码！**

---

## 🚀 第五步：启动服务

### 5.1 测试运行

先测试一下服务是否能正常启动：

```bash
npm run build
npm run start
```

如果看到类似以下信息，说明启动成功：
```
> Ready on http://0.0.0.0:3000
> Socket.IO server running at ws://0.0.0.0:3000/api/socketio
```

**此时不要关闭这个窗口！** 打开浏览器，访问 `http://你的服务器IP:3000`，应该能看到文件系统页面。

### 5.2 使用PM2保持服务运行

为了确保服务在关闭SSH窗口后仍然运行，我们需要安装PM2：

```bash
npm install -g pm2
```

然后创建启动脚本：

```bash
pm2 start "npm run start" --name "file-system"
```

设置PM2开机自启：

```bash
pm2 startup
pm2 save
```

查看服务状态：

```bash
pm2 status
```

如果看到 `file-system` 的状态是 `online`，说明服务正常运行。

---

## 🌐 第六步：配置域名（可选）

如果你有域名，可以配置域名访问。

### 6.1 安装Nginx

**Ubuntu 系统：**
```bash
apt install -y nginx
```

**CentOS 系统：**
```bash
yum install -y nginx
```

### 6.2 配置Nginx

创建Nginx配置文件：

```bash
nano /etc/nginx/sites-available/file-system
```

在打开的文件中粘贴以下内容（把 `your-domain.com` 改成你的域名）：

```nginx
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

按 `Ctrl+X`，然后按 `Y`，再按回车保存。

### 6.3 启用配置

```bash
ln -s /etc/nginx/sites-available/file-system /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
systemctl enable nginx
```

### 6.4 配置SSL（可选，但推荐）

安装Certbot：

**Ubuntu 系统：**
```bash
apt install -y certbot python3-certbot-nginx
```

**CentOS 系统：**
```bash
yum install -y certbot python3-certbot-nginx
```

申请SSL证书：

```bash
certbot --nginx -d your-domain.com -d www.your-domain.com
```

按照提示操作，选择重定向到HTTPS。

---

## 🔧 第七步：防火墙配置

### 7.1 开放端口

**Ubuntu 系统（UFW）：**
```bash
ufw allow 22
ufw allow 80
ufw allow 443
ufw enable
```

**CentOS 系统（Firewalld）：**
```bash
firewall-cmd --permanent --add-service=ssh
firewall-cmd --permanent --add-service=http
firewall-cmd --permanent --add-service=https
firewall-cmd --reload
```

### 7.2 云服务器安全组

登录你的云服务器控制台，在安全组设置中开放以下端口：
- **22端口**（SSH连接用）
- **80端口**（HTTP访问用）
- **443端口**（HTTPS访问用）

---

## 🎉 第八步：最终测试

### 8.1 访问测试

1. 在浏览器中访问：
   - 如果没有域名：`http://你的服务器IP`
   - 如果有域名：`https://你的域名`

2. 应该能看到文件系统页面

3. 点击"管理员登录"，使用 `.env` 中设置的管理员账号登录：
   - 用户名：`ADMIN_USERNAME`
   - 密码：`ADMIN_PASSWORD`

### 8.2 功能测试

1. **上传文件测试**：
   - 登录后点击"上传文件"
   - 选择一个小文件（比如图片）
   - 添加描述信息
   - 点击上传

2. **下载文件测试**：
   - 回到首页（不登录）
   - 找到刚上传的文件
   - 点击下载按钮

3. **命令行下载测试**：
   - 在你的电脑上打开命令行
   - 输入：`wget "http://你的服务器IP/api/files/文件ID/download" -O test.txt`
   - （文件ID可以在浏览器开发者工具中找到）

---

## 🆘 常见问题解决

### 问题1：连接服务器失败
- **检查**：IP地址是否正确
- **检查**：服务器是否启动
- **检查**：安全组是否开放22端口

### 问题2：npm install 失败
- **解决方案**：
  ```bash
  npm cache clean --force
  npm install
  ```

### 问题3：端口3000被占用
- **查看占用**：
  ```bash
  netstat -tlnp | grep 3000
  ```
- **杀死进程**：
  ```bash
  kill -9 进程ID
  ```

### 问题4：PM2命令找不到
- **解决方案**：
  ```bash
  npm install -g pm2
  ```

### 问题5：Nginx配置错误
- **检查配置**：
  ```bash
  nginx -t
  ```
- **查看错误日志**：
  ```bash
  tail -f /var/log/nginx/error.log
  ```

---

## 📞 需要帮助？

如果按照以上步骤还是无法成功部署，请：

1. **截图保存错误信息**：把命令行中的错误信息截图保存
2. **记录你的环境**：
   - 服务器系统（Ubuntu/CentOS）
   - 服务器IP
   - 执行到哪一步出错
3. **联系技术支持**：把以上信息发给懂技术的朋友或云服务器客服

---

## 🎯 部署成功后的使用

### 管理员操作
1. 访问你的域名或IP
2. 点击"管理员登录"
3. 使用 `.env` 中设置的管理员账号登录
4. **妥善保管管理员密码**（重要！）
5. 开始上传文件

### 普通用户操作
1. 直接访问域名或IP
2. 浏览文件
3. 点击下载
4. 或使用 wget/curl 下载

### 修改管理员密码
目前需要手动修改数据库或重新创建管理员，建议联系技术人员协助。

---

**恭喜！如果你按照以上步骤操作成功，你的文件分享系统就已经部署完成了！** 🎉
